const path = require("path")

module.exports = {
  entry: {
    app: "./<%= clientFolder %>/index.ts",
  },
  resolve: {
    // Add '.ts' and '.tsx' as resolvable extensions.
    extensions: [".ts", ".tsx", ".js", ".jsx"],
  },
};
